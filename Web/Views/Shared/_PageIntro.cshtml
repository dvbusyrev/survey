@model MainProject.Web.ViewModels.PageIntroModel

@if (!string.IsNullOrWhiteSpace(Model?.Title))
{
    <header class="app-page__intro app-page__intro--title-only">
        <h1 class="app-page__title">@Model.Title</h1>
    </header>
}
