@model IReadOnlyList<MainProject.Application.DTO.SelectionOption>

@{
    var organizations = Model ?? Array.Empty<MainProject.Application.DTO.SelectionOption>();
}

<div id="addUserModal" class="modal">
    <div class="modal-content">

        <div class="modal-header">
            <h2 class="h2_modal">Добавление пользователя</h2>
            <button type="button" class="modal-close" data-modal-close="addUserModal"><i class="fas fa-xmark"></i></button>
        </div>
        <div class="modal-body">
            <section class="app-section app-page__section app-page__stack">
                <div class="app-field-group">
                    <label for="fullName">ФИО</label>
                    <input type="text" id="fullName" class="app-field" placeholder="Введите ФИО" required>
                </div>
                <div class="app-field-group">
                    <label for="username">Логин</label>
                    <div class="app-field-with-icon">
                        <div class="icon-container field-icon-container"><i class="fas fa-user-group"></i></div>
                        <input type="text" id="username" class="app-field" placeholder="Введите логин" required>
                    </div>
                </div>
                <div class="app-field-group">
                    <label for="password">Пароль</label>
                    <div class="app-field-with-icon has-toggle">
                        <div class="icon-container field-icon-container"><i class="fas fa-lock"></i></div>
                        <input type="text" id="password" class="app-field is-password-masked" data-password-field="true" data-password-visible="false" autocomplete="new-password" inputmode="text" lang="ru" autocapitalize="none" spellcheck="false" placeholder="Введите пароль" required>
                        <button type="button" class="password-toggle-btn" aria-label="Показать пароль" title="Показать пароль"></button>
                    </div>
                </div>
                <div class="app-field-group">
                    <label for="email_input">Эл. почта</label>
                    <div class="app-field-with-icon">
                        <div class="icon-container field-icon-container"><i class="fas fa-envelope"></i></div>
                        <input type="email" id="email_input" class="app-field" placeholder="Введите эл. почту">
                    </div>
                </div>
                <div class="app-field-group">
                    <label for="userOrganization">Организация</label>
                    <div class="app-field-with-icon">
                        <div class="icon-container field-icon-container"><i class="fas fa-building"></i></div>
                        <select id="userOrganization" class="app-field" required>
                            <option value="" selected disabled>Выберите организацию</option>
                            @foreach (var org in organizations)
                            {
                                <option value="@org.Id">@org.Name</option>
                            }
                        </select>
                    </div>
                </div>
                <div class="app-field-group">
                    <label for="userRole">Роль</label>
                    <select id="userRole" class="app-field" required>
                        <option value="" selected disabled>Выберите роль</option>
                        <option value="user">Клиент</option>
                        <option value="admin">Администратор</option>
                    </select>
                </div>
                <div class="app-field-group">
                    <label for="dateBegin">Дата начала</label>
                    <input type="date" id="dateBegin" class="app-field" data-date-native="true" data-date-label="Дата начала" lang="ru-RU">
                </div>
                <div class="app-field-group">
                    <label for="dateEnd">Дата конца</label>
                    <input type="date" id="dateEnd" class="app-field" data-date-native="true" data-date-label="Дата конца" lang="ru-RU">
                </div>
                @Html.AntiForgeryToken()
            </section>
        </div>
        <div class="modal-footer">
            <button type="button" data-modal-close="addUserModal" class="app-button app-button--secondary">Отмена</button>
            <button type="button" data-click-call="submitFormAdd" class="app-button app-button--primary">Сохранить</button>
        </div>
    </div>
</div>

<div id="editUserModal" class="modal">
    <div class="modal-content">
        <div class="modal-header">
            <h2 class="h2_modal">Редактирование пользователя</h2>
            <button type="button" class="modal-close" data-modal-close="editUserModal"><i class="fas fa-xmark"></i></button>
        </div>
        <div class="modal-body">
            <section class="app-section app-page__section app-page__stack">
                <input type="hidden" id="editUserId">
                <div class="app-field-group">
                    <label for="editFullName">ФИО</label>
                    <input type="text" id="editFullName" class="app-field" placeholder="Введите ФИО" required>
                </div>
                <div class="app-field-group">
                    <label for="editUsername">Логин</label>
                    <div class="app-field-with-icon">
                        <div class="icon-container field-icon-container"><i class="fas fa-user-group"></i></div>
                        <input type="text" id="editUsername" class="app-field" placeholder="Введите логин" required>
                    </div>
                </div>
                <div class="app-field-group">
                    <label for="editPassword">Новый пароль</label>
                    <div class="app-field-with-icon has-toggle">
                        <div class="icon-container field-icon-container"><i class="fas fa-lock"></i></div>
                        <input type="text" id="editPassword" class="app-field is-password-masked" data-password-field="true" data-password-visible="false" autocomplete="new-password" inputmode="text" lang="ru" autocapitalize="none" spellcheck="false" placeholder="Введите новый пароль или оставьте поле пустым">
                        <button type="button" class="password-toggle-btn" aria-label="Показать пароль" title="Показать пароль"></button>
                    </div>
                </div>
                <div class="app-field-group">
                    <label for="editEmail">Эл. почта</label>
                    <div class="app-field-with-icon">
                        <div class="icon-container field-icon-container"><i class="fas fa-envelope"></i></div>
                        <input type="email" id="editEmail" class="app-field" placeholder="Введите эл. почту">
                    </div>
                </div>
                <div class="app-field-group">
                    <label for="editUserOrganization">Организация</label>
                    <div class="app-field-with-icon">
                        <div class="icon-container field-icon-container"><i class="fas fa-building"></i></div>
                        <select id="editUserOrganization" class="app-field" required>
                            <option value="" disabled>Выберите организацию</option>
                            @foreach (var org in organizations)
                            {
                                <option value="@org.Id">@org.Name</option>
                            }
                        </select>
                    </div>
                </div>
                <div class="app-field-group">
                    <label for="editUserRole">Роль</label>
                    <select id="editUserRole" class="app-field" required>
                        <option value="" disabled>Выберите роль</option>
                        <option value="user">Клиент</option>
                        <option value="admin">Администратор</option>
                    </select>
                </div>
                <div class="app-field-group">
                    <label for="editDateBegin">Дата начала</label>
                    <input type="date" id="editDateBegin" class="app-field" data-date-native="true" data-date-label="Дата начала" lang="ru-RU">
                </div>
                <div class="app-field-group">
                    <label for="editDateEnd">Дата конца</label>
                    <input type="date" id="editDateEnd" class="app-field" data-date-native="true" data-date-label="Дата конца" lang="ru-RU">
                </div>
                @Html.AntiForgeryToken()
            </section>
        </div>
        <div class="modal-footer">
            <button type="button" data-modal-close="editUserModal" class="app-button app-button--secondary">Отмена</button>
            <button type="button" data-click-call="updateUser" class="app-button app-button--primary">Сохранить</button>
        </div>
    </div>
</div>

<div id="deleteUserModal" class="modal">
    <div class="modal-content">
        <div class="modal-header">
            <h2 class="h2_modal">Удаление пользователя</h2>
            <button type="button" class="modal-close" data-modal-close="deleteUserModal"><i class="fas fa-xmark"></i></button>
        </div>
        <div class="modal-body">
            <section class="app-section app-page__section app-page__stack">
                <p>Вы действительно хотите удалить пользователя <span id="deleteUserName"></span>?</p>
                <input type="hidden" id="deleteUserId">
                @Html.AntiForgeryToken()
            </section>
        </div>
        <div class="modal-footer">
            <button type="button" data-modal-close="deleteUserModal" class="app-button app-button--secondary">Отмена</button>
            <button type="button" data-click-call="deleteUserFromModal" class="app-button app-button--danger">Удалить</button>
        </div>
    </div>
</div>
