
- проверить плагин

Сделать нормальные роли в БД
    - доп. таблица role с ролями (admin, user)
    - в таблице user указать id role
    - сделать изменения в коде 
    
- Проверки “свой/чужой” доступ
Сейчас [Authorize] и роли уже сильно лучше, но ещё нужно добить места, где пользователь открывает данные по id в URL.
Например:
 • survey_list_user/{id}
 • просмотр/редактирование ответов
 • подписание/отчёты по idSurvey, idOmsu

Там нужен принцип:
 • админ может всё
 • обычный пользователь — только своё

Иначе останется риск IDOR.


4. CSRF-защита на POST
Теперь, когда у вас cookie auth, это особенно важно.
Нужно пройтись по POST-методам, которые:
 • создают
 • удаляют
 • обновляют

И добавить антифорджери туда, где её нет.

5. Убрать Html.Raw(...) из inline JS
Особенно в местах, где пользовательские данные вставляются в onclick и подобные штуки.
Это реальный кандидат на XSS.

6. Нормализовать list_surveys
Если связь анкет и ОМСУ всё ещё хранится строкой — это нужно убирать.
Сделать отдельную таблицу связей.
Это улучшит:
 • корректность
 • безопасность логики
 • производительность SQL

8. Ускорение тяжёлых страниц
После логина у вас всё ещё тяжёлые экраны.
Я бы дальше чинил:
 • Babel в браузере
 • ленивую загрузку тяжёлых библиотек
 • response compression, если ещё не везде применили
 • тяжёлые SQL/статистику

9. Логи
Убрать из логов:
 • хэши паролей
 • чувствительные данные
 • лишние stack trace наружу

10. Единый current user/access service
Сейчас хороший момент сделать один сервис, через который везде проверяется:
 • кто пользователь
 • админ ли он
 • имеет ли доступ к сущности
